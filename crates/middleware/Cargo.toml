[package]
name = "alternate-middleware"
version = "0.1.0"
edition = "2024"

[dependencies]
alternate-http.workspace = true

bytes = "1.12.1"
http = "1.5.0"
tower = { version = "0.5.3", features = ["buffer"] }
thiserror.workspace = true

[lints]
workspace = true
